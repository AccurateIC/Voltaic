/** Canonical React Query key for `tuyau.archive.getAnomalyStatistics` — use everywhere to avoid duplicate fetches. */
export const anomalyStatisticsQueryKey = ["archive", "get-anomaly-statistics"] as const;
